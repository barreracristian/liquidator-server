angular.module('liquidator-server.controllers.SiniestroController', [])
    .controller('SiniestroController',
        function ($scope, $http, $stateParams) {

            $scope.titulo = "siniestro";
        }
    )
;
